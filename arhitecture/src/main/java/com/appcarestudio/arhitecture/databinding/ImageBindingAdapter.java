package com.appcarestudio.arhitecture.databinding;

public final class ImageBindingAdapter {
/*
    @BindingAdapter(value = "url,placeholder")
    public static void loadImageUrl(ImageView view, String url,  String placeholder) {
        if (url != null && !url.equals(""))
            Picasso.get()
                    .load(url)
                  //  .placeholder(placeholder)
                    .into(view);
    }*/

}
